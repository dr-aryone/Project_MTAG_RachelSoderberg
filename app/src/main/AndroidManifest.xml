<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="guide.athletes.thai.muay.project_mtag_rachelsoderberg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SkillsActivity" />
        <activity android:name=".CombosActivity" />
        <activity android:name=".BoxingActivity" />
        <activity android:name=".KickingActivity" />
        <activity android:name=".AllWeaponsActivity" />
        <activity android:name=".StylesActivity" />
        <activity android:name=".SelfDefenseActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".WorkoutsActivity" />
        <activity android:name=".WodsActivity" />
        <activity android:name=".TimerActivity"></activity>
    </application>

</manifest>